services:
  backend:
    build:
      context: .
      dockerfile: stocky-api/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev

  frontend:
    build: ./stocky-web
    ports:
      - "3001:80"
    depends_on:
      - backend
